name: Deploy BoxityERP V.0.1 to SSH Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest

    steps:
    - name: Checkout repository
      uses: actions/checkout@v3

    - name: Deploy files via SCP
      run: |
        rsync -avz --delete ./ boxity@153.92.5.213:/demo-erp-v01.boxity.id
